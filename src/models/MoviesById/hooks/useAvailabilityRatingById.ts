export const useAvailabilityRatingById = (
	allDelileredRating: Map<number, number>,
	id: number | undefined
) => {
	// let isAvailabilityRating: boolean = false
	// allDelileredRating.forEach(items => {
	// 	if (id && items.has(id)) {
	// 		isAvailabilityRating = true
	// 	}
	// 	return isAvailabilityRating
	// })
}
// allDelileredRating.forEach(items => {
//   if (id && items.has(id)) {
//     setIsAvailabilityRating(true)
//   }
// })
